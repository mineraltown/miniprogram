<!--pages/setting/setting.wxml-->
<mp-cells title="设置">
    <picker bindchange="changeVersion" value="{{ index }}" range="{{ version }}" range-key="label">
        <mp-cell value="切换游戏版本" link="true" footer="{{ version[index].label }}"></mp-cell>
    </picker>
</mp-cells>